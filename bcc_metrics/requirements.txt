# Core runtime and scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
pillow>=9.0

# Deep learning
# Required by the PyTorch training and evaluation scripts.
torch>=2.0
torchvision>=0.15

# Metrics and reporting
# pingouin and psutil have fallbacks in the metrics code, while tabulate is
# needed by pandas.DataFrame.to_markdown().
pingouin>=0.5
psutil>=5.9
tabulate>=0.9

# ONNX inference and conversion
# onnx2tf is only needed when exporting EfficientDet-Lite0 to TFLite.
onnxruntime>=1.16
onnx2tf

# Shared training utilities
plotly
tqdm
kaleido

# Model-specific training packages
# YOLOv8n, YOLO11n, and RT-DETR-nano.
ultralytics

# EfficientDet-Lite0.
effdet
timm

# RTMDet-tiny bootstrap tool. OpenMMLab packages must be installed with mim
# because compatible wheels depend on the active Python/CUDA setup.
openmim

# After installing this file, install RTMDet-tiny dependencies with:
#   mim install mmengine "mmcv>=2.0.0" "mmdet>=3.0.0" "mmyolo>=0.6.0"
